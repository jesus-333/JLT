# Hyperparameters for the DecisionTreeClassifier.
#
# Used only when general.toml has `classifier = "decision_tree"`. Only the VALUES
# may change between rounds ; the set of keys must stay intact.
# See sklearn.tree.DecisionTreeClassifier for the meaning of each option.

max_depth         = 0        # maximum tree depth ; 0 means no limit (None)
min_samples_split = 2        # minimum samples required to split an internal node
min_samples_leaf  = 1        # minimum samples required at a leaf node
criterion         = "gini"   # split quality : gini | entropy | log_loss
max_features      = "all"    # features considered per split : all | sqrt | log2
