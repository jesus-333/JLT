# Extra dependencies needed to RUN the example experiments (not needed to merely
# register them with `jlt autoresearch add`, which only parses run.py statically).
#
# Install the CPU build of torch if you do not have a GPU, e.g. :
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
torch
torchvision
