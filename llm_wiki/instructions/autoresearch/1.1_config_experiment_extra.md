When a new experiment is added to the register also another stuff must be checked

Inside the experiment folder there should be a file called `experiment_description`.
This file could be both `txt` or `md`, and it should contain a complete description of the experiment, its purpose, and what it wants to achieve
